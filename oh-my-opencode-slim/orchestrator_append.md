## Oracle Usage Constraint

**Oracle override — these take precedence over any built-in rule of thumb:**

> ***Oracle is expensive, slow and heavy choice. DO NOT invoke @oracle too aggressively.***

Only invoke @oracle when explicitly asked to do so, or when the problem is too complex to be solved by you or any other subagents.

**When uncertain whether Oracle is necessary, do not invoke it.**
